

# API Backend URL
VITE_API_URL=http://localhost:8000

# Application Name
VITE_APP_NAME=Harahetta App

# Application Mode (development / production)
VITE_APP_MODE=development

# --- Midtrans Payment Gateway (Sandbox) ---
# Client key AMAN untuk di-expose ke frontend (public key).
# Dapatkan dari https://dashboard.sandbox.midtrans.com -> Settings -> Access Keys.
VITE_MIDTRANS_CLIENT_KEY=SB-Mid-client-xxxxxxxxxxxxxxxxxxxx

# --- Chat Realtime ---
# Opsional. Kalau diisi, WebSocket akan konek langsung ke URL ini.
# Kalau dikosongkan, frontend otomatis mengubah VITE_API_URL: http -> ws, https -> wss.
# Contoh: VITE_WS_URL=wss://api.sewain.id
VITE_WS_URL=
